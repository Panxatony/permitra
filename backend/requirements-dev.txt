# Test- und Prüfwerkzeuge (nicht im Laufzeit-Image, nur für CI und lokale Entwicklung)
-r requirements.txt

pytest>=8.3
pytest-cov>=6.0
httpx>=0.28          # TestClient für HTTP-/Autorisierungstests
ruff>=0.8            # Linting
pip-audit>=2.7       # bekannte Schwachstellen in Abhängigkeiten
