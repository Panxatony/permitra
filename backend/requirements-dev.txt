# Test and linting tools (not part of the runtime image, only for CI and local development)
-r requirements.txt

pytest>=8.3
pytest-cov>=6.0
httpx2>=2.12         # TestClient for the HTTP/authorization tests
                     # (starlette deprecates plain httpx here)
ruff>=0.8            # Linting
pip-audit>=2.10.1       # known vulnerabilities in the dependencies
