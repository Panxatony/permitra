"""The one place the version number lives.

It was written out twice - FastAPI metadata and frontend package.json - and a
footer would have made it three. Two copies of a version drift the week after
someone forgets one; the footer now shows what the backend answers, so the
number on screen is the number of the code that is actually running.
"""

VERSION = "0.7.4-alpha"
